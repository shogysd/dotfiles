SHELL_NAME='bash'

if [ $(echo $SHELL | xargs basename) = 'bash' ]; then
    $(which zsh >/dev/null 2>&1)
    if [ $? = 0 ]; then
        exec zsh
    fi
fi

if [ -f ~/dotfiles/bash/bashrc ]; then
    . ~/dotfiles/bash/bashrc
else
    echo "~/dotfiles/bash/bashrc : No such file"
fi
